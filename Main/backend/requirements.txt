# Core parsing
PyMuPDF>=1.23.0
python-docx>=1.1.0
spacy>=3.7.0
# spaCy model (install separately):
#   python -m spacy download en_core_web_sm

# Matching / scoring (added in deterministic phases)
sentence-transformers>=2.0.0  # SBERT semantic embeddings
scikit-learn>=1.3.0           # TF-IDF embedding fallback
numpy>=1.24.0

# LLM layer
httpx>=0.27.0    # HTTP client for Gemini REST API
pydantic>=2      # Structured-output schemas + validation
# Optional — Groq SDK (cloud backend), lazy-imported on demand:
#   pip install groq

# API layer (Phase 4 — FastAPI HTTP service)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0   # ASGI server: uvicorn app.main:app
python-multipart>=0.0.9     # multipart/form-data file uploads

# Test runner
pytest>=8.0.0
